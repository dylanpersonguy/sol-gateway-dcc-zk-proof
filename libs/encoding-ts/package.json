{
  "name": "@sol-bridge/encoding-ts",
  "version": "2.0.0",
  "description": "Canonical encoding library for SOL⇄DCC ZK Bridge",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "test": "npx vitest run",
    "test:vectors": "npx vitest run --reporter=verbose"
  },
  "dependencies": {
    "@noble/hashes": "^1.3.0"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
